name: Manual Approval Workflow

on:
  push:
    branches:
      - main   # or dev, whatever branch contributors push to

jobs:
  hold_for_approval:
    name: Wait for Admin Approval
    runs-on: ubuntu-latest
    environment: 
      name: staging-approval   # must match the environment you created in Settings → Environments
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Wait for Approval
        run: echo "This job will run only after admin approves."
